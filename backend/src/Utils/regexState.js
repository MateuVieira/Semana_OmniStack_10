module.exports = function regexState(data) {

    console.log(data);

    // const info = data.split('of').map(estado => estado.trim());

    // const result = info[1].split(', ').map(estado => estado.trim())

    // console.log(result);

    return {state: 0, country: 0};
}